# JWT Secret Key
# Generate a new one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here

# Database URL (PostgreSQL via Docker)
# Default for the project setup
DATABASE_URL=postgresql+asyncpg://postgres:123456@localhost:5432/postgres
